services:
  - type: web
    name: weather-forecast-app
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn weather_forecast_app:app --host 0.0.0.0 --port 10000
    autoDeploy: true
